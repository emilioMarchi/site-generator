{
  "name": "site-generator",
  "version": "1.0.0",
  "description": "Generador de sitios web a partir de configuraciones",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "generate": "npm run build && node dist/index.js",
    "generate:example": "npm run build && node dist/index.js generate --config=config/examples/escuela-musica.json",
    "queue:status": "npm run build && node dist/index.js queue:status",
    "queue:process": "npm run build && node dist/index.js queue:process"
  },
  "keywords": [
    "site-generator",
    "web",
    "nextjs",
    "template"
  ],
  "author": "Emilio Marchi",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "chalk": "^5.3.0",
    "commander": "^11.1.0",
    "dotenv": "^16.4.5",
    "firebase-admin": "^13.6.1",
    "fs-extra": "^11.2.0",
    "mammoth": "^1.8.0",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
